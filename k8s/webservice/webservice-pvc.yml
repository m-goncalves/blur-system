apiVersion: v1
kind: PersistentVolumeClaim
metadata: 
  name: webservice-pvc
spec:
  storageClassName: local
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      type: local
      app: blur